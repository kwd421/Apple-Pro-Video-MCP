{
  "name": "apple-pro-video-mcp",
  "version": "0.3.0",
  "description": "Local MCP server for Vibe transcript imports, Final Cut Pro FCPXML, edit planning, subtitles, and Motion template workflows on macOS.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "bin": {
    "apple-pro-video-mcp": "./src/index.js"
  },
  "scripts": {
    "start": "node src/index.js",
    "inspect": "npx @modelcontextprotocol/inspector node src/index.js",
    "test": "node --test test/*.test.mjs",
    "lint": "node --check src/index.js && node --check src/server.js && node --check src/transcripts.js && node --check src/editing.js && node --check src/subtitles.js && node --check src/fcpxml.js && node --check src/motion.js && node --check src/system.js && node --check src/xml.js && node --check src/paths.js",
    "check": "npm run lint && npm test"
  },
  "keywords": [
    "mcp",
    "final-cut-pro",
    "motion",
    "fcpxml",
    "subtitles",
    "transcription",
    "video-editing",
    "macos"
  ],
  "dependencies": {
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.0.0"
  }
}
